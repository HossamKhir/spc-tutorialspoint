import React from 'react';
import Counter from './container/counterContainer';
import './App.css';

const APP = (props) => (
    <div className='App'>
        <header className='App-header'>
            <Counter />
        </header>
    </div>
);

export default APP;
